---
import { config } from '../config';
---

<section id="contact" class="py-20 w-full bg-slate-950 text-center text-slate-100">
  <div class="container mx-auto px-6 max-w-5xl">
    <div class="max-w-2xl mx-auto">
     <h2 class="text-blue-500 font-mono mb-4 text-sm tracking-widest">What's Next?</h2>
     <h2 class="text-4xl md:text-5xl font-bold text-slate-100 mb-6">Get In Touch</h2>
     <p class="text-slate-400 text-lg mb-12">
       Although I'm not currently looking for any new opportunities, my inbox is always open. Whether you have a question or just want to say hi, I'll try my best to get back to you!
     </p>
     <a href={`mailto:${config.email}`} class="inline-block px-8 py-4 border border-blue-500 text-blue-500 font-medium rounded hover:bg-blue-500/10 transition-colors duration-200">
       Say Hello
     </a>
  </div>
  
  <footer class="mt-32 flex flex-col items-center gap-6">
    <div class="flex gap-6">
      {Object.entries(config.socials).map(([platform, url]) => (
        <a href={url} target="_blank" rel="noopener noreferrer" class="text-slate-400 hover:text-blue-500 hover:-translate-y-1 transition-all duration-200">
          <span class="sr-only">{platform}</span>
          <span class="capitalize font-mono text-sm">{platform}</span>
        </a>
      ))}
    </div>
  </footer>
  </div>
</section>
